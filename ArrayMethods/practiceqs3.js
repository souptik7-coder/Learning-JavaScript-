//  Create a new array whose elements are in uppercase of words present in the  original array
let char=["campushub","souptik","youtube","channel"];
let upper=char.map((el)=>{
    return el.toUpperCase();
})
console.log(upper);
