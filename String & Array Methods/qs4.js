// Write a JavaScript program to test whether the character at the given (character) index is lower case.
let ch="apple";
let index=3;
if(ch[index]==ch[index].toLowerCase()){
    console.log("The given index is lowercase");
}else{
    console.log("The given index is not lower case");
}
